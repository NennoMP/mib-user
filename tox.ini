[metadata]
license_file = LICENSE

[bdist_wheel]
universal = True

[testenv]
passenv = TRAVIS TRAVIS_JOB_ID TRAVIS_BRANCH

[pytest]
testpaths =
    tests
log_cli = True
addopts =
    --cov=mib
    --cov-fail-under=90
    -r a
    -v

[coverage:run]
source =
    mib
omit =

